package:
  fail2ban:
    installed: true
  tailscale:
    installed: true

service:
  ssh:
    running: true
  fail2ban:
    enabled: true
  tailscaled:
    enabled: true

file:
  /etc/ssh/sshd_config:
    exists: true
    mode: "0600"
    owner: root
    group: root
